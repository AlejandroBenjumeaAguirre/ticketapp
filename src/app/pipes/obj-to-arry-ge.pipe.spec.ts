import { ObjToArryGEPipe } from './obj-to-arry-ge.pipe';

describe('ObjToArryGEPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjToArryGEPipe();
    expect(pipe).toBeTruthy();
  });
});
